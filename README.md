# BitPacking Project

Objectif : compresser des tableaux d'entiers (bit-packing) pour accélérer la transmission.

## Installation
1. python -m venv .venv
2. .venv\Scripts\Activate.ps1
3. pip install -r requirements.txt

## Utilisation
Afficher l'aide :
    python -m cli.bitpacking_cli --help
